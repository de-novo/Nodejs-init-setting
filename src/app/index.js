import { Router } from "express";

export default () => {
    const app = Router();
    /*
        routing 
        import userRoute from './User/userRoute.js'
        ex => userRoute(app)
    */
    return app;
};
